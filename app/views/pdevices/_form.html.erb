<%= form_for(@pdevice) do |f| %>
  <% if @pdevice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pdevice.errors.count, "error") %> prohibited this pdevice from being saved:</h2>

      <ul>
      <% @pdevice.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :token %><br />
    <%= f.text_area :token,:rows=>5,:cols=>100 %>
  </div>
  <div class="field">
    <%= f.label :flag %><br />
    <%= f.number_field :flag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
